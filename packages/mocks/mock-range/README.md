# @manta-style/mock-range

## Installation

This plugin is included by default

## Usage

```js
/**
 * @range {min} {max}
 */
value: number;
```

- For numbers only
- Generate random number between `{min}` and `{max}`
